package alg.laioffer.class23.adv1ArrayLCA;

/**
 * For each group of elements with the same value do not keep any of them.
 * {1, 2, 2, 3, 3, 3} → {1}
 */
public interface ArrayDedupThree {
  int[] dedup(int[] array);
}
